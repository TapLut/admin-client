export enum OrderStatus {
  PENDING = 'pending',
  CONFIRMED = 'confirmed',
  PROCESSING = 'processing',
  SHIPPED = 'shipped',
  DELIVERED = 'delivered',
  READY = 'ready',
  REDEEMED = 'redeemed',
  CANCELLED = 'cancelled',
}
